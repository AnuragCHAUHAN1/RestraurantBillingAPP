import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ravisaoji.billing',
  appName: 'Ravi Saoji Billing',
  webDir: 'dist'
};

export default config;
